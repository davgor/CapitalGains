# 010.5 — Live broker adapter (disabled by default)

Implement live broker behind the same port as paper (Alpaca or chosen API). Default config remains paper. No orders unless arming gate passes (010.6).

## Acceptance criteria

- [ ] Live adapter implements broker port methods
- [ ] Feature flag / config `executionMode: paper | live` defaults to paper
- [ ] Integration test mocked at HTTP layer; no real orders in CI
- [ ] Runbook documents credentials and kill switch
