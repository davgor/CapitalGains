# 007.7 — Stage machine + SitOut + multi-name purchases

Orchestrator advances stages with durable commits. Hardcoded or injected Research plan → risk check → Purchases executes **every** allocation line. `sitOut: true` or empty allocations skips purchases but continues to Outcome/Lessons placeholders.

## Acceptance criteria

- [ ] Stage transitions persist before advancing; illegal jumps rejected
- [ ] SitOut path creates Outcome with 100% cash / zero deployed without fills
- [ ] Multi-name plan (e.g. NVDA + GOOGL weights) yields one fill row per symbol
- [ ] Off-tape symbols rejected even if present in plan
- [ ] Unit tests cover happy path, SitOut, and risk rejection
