# 008.8 — Usage metering

Record per-run / per-factory token usage and cost when SDK provides it (`getUsage` or stream events). Surface totals for Settings/Daily views later.

## Acceptance criteria

- [ ] Usage rows persisted keyed by factory + session + stage
- [ ] Missing usage from mock/local still leaves null cost without crashing
- [ ] Helper aggregates daily SDK spend across factories
- [ ] Unit tests with fixture usage payloads
