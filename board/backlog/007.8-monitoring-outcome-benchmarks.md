# 007.8 — Monitoring loop + Outcome dual benchmarks

Every 2 minutes refresh marks and unrealized deltas. At close, flatten with friction, compute gross and **net** realized P&L, return on **full daily limit** and on **deployed capital**, and SPY same-session benchmark aligned to fill/flatten window.

## Acceptance criteria

- [ ] Monitor tick updates snapshots idempotently for a frozen clock sequence
- [ ] Outcome stores gross, net, full-limit return, deployed return, SPY return
- [ ] Aggregate “Daily Profit” helpers use **net** only
- [ ] Unit tests with fixture prices prove dual benchmarks differ when cash residual exists
