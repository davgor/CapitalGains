# 007.10 — Hardcoded full-day integration test

End-to-end (no SDK): inject clock + mock market data, run one factory through a full session with a hardcoded multi-name basket, assert fills, monitor snapshots, net Outcome vs SPY fixtures, and restart resume mid-day.

## Acceptance criteria

- [ ] Integration/unit harness drives open→close without Cursor SDK
- [ ] Asserts multi-name fills, residual cash, net P&L, dual benchmarks
- [ ] Asserts resume after simulated restart during monitoring
- [ ] `npm test` covers this path; documented as Phase 1 proof in a short `docs/runbooks/` note
