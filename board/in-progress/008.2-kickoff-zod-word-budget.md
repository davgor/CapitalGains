# 008.2 — Kickoff Zod contract + word budget

Kickoff output schema: hypothesis, style, search directive, ≥2 negative constraints, `allow_full_cash`, `generated_kickoff_prompt`. Soft ≤300 words; hard stop 350; one compress retry; else Failed/BudgetExceeded.

## Acceptance criteria

- [ ] Zod parse rejects invalid Kickoff payloads (unit-tested)
- [ ] Word-count validator triggers compress retry path once, then BudgetExceeded
- [ ] System prompt text includes compression/pruning rules (checked into repo as fixture/constant)
- [ ] Valid Kickoff artifact persisted for modal replay
