# 009.1 — App shell header + Daily Limit / Profit

Match wireframe header: brand `CapitalGains`, editable Daily Limit, aggregated Daily Profit (**net**). Wire to store/orchestrator.

## Acceptance criteria

- [ ] Header renders Limit + Profit; Limit edits persist for next allocation cycle
- [ ] Profit aggregates factory net outcomes for the session day
- [ ] Component/unit tests for aggregation helper
- [ ] Layout usable on desktop window sizes used by Electron
