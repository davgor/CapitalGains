# 008.5 — Tape-bounded Research agent

Research agent ingests kickoff + feature tape; optional directed search; emits Zod plan with `sitOut` or multi-name weights. Symbols must ⊆ tape. Risk caps applied before Purchases.

## Acceptance criteria

- [ ] Research Zod schema matches plan (`sitOut`, allocations[], required stopLossPercent)
- [ ] Off-tape symbol rejected (unit-tested) even if agent emits it
- [ ] Empty/sitOut plans skip Purchases via existing 007 path
- [ ] One schema retry then Failed/Skipped
